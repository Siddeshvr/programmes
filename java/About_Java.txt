import java.util.Scanner;
    import java.util.Arrays;
    import java.util.ArrayList;
    import java.util.LinkedList;
    import java.util.Random;
import java.lang.Math;
    import java.lang.System;

import java.io.IOException;

class name and file_name must be same...

public static void main(String[] ars){}   //main module...

System.out.println();    //in next line...
    System.out.print();  //in same line...
char c =(char) System.in.read();    //to read a character value only...It will throw IOException...use throws IOException{ write code here}...java.io.IOException;

Scanner jin = new Scanner(System.in);  //import java.util.Scanner...and create a object inside main...
    jin.close();  //use at the end of main...
    jin.nextInt(),jin.nextLong(),jin.nextDouble(),jin.nextLine();
    jin.next().charAt(0);   //to read character...

String c = new String(S);    //char_array 'S' to String c...

CharSequence a = Character.valueOf(x).toString();    //from character 'x' to char_sequence...
    if(S.contains(a)){}     //Here S is String and a is a char_sequence...return boolean value...

String x = a.toString();   //char_sequence to String...
    char x = a.charAt(0);  //char_sequence to character...

int z = c.indexOf(x);      //index of 'x' in String c...

C = C.replaceFirst(String.valueOf(C.charAt(z)),"");    //remove x in string C... or replace x from other char in String C...z is index of x...

char[] C1  = Arrays.copyOfRange(C,1,B.length());      //remove 0th position char from C...here B.length for to get new C1 length...

char[] C = jin.nextLine().toCharArray();              //String to char_array...here input is String...

jin.nextLine();      //necessary before reading String or char_array...

ArrayList<Integer> stack = new ArrayList<Integer>(n);    // stack is a Array_List...
      stack.add(x);
      stack.isEmpty();
      int x = stack.remove(index);

int x=0,y=0,z=0;

Math.pow(2,j);    //2^j  : import java.lang.Math...
    Math.cbrt(x);
    Math.sqrt(x);
    Math.log(x);
    Math.hypot(a,b);  //hypotenuse of right angle triangle...
    Math.abs(x);   //To find absolute value of x...

we don't have pointers in java...

(int)S[i] , (int)a , (char)a;

S[i] += 4;   //if S[i]='a' then will become 'e'...
    S[i] = S[i]+4;   //Invalid...

return a>b?a:b;

fn(C , S.length());        //while invoking use S.length()...
    for(int i=0;i<S,length;i++)    //while iterating use S.length...

Arrays.sort(A);   //A is unsorted...java.util.Arrays...

x[i] = swap(x[l] , x[l]=x[i]);  //invoking explicit swap module... 
    swap(a,b){ return a; }      
    we can't use swap like c++...

String C = Arrays.toString(S);    //S is Char_Array...S to List or arrayList...

Random R = new Random();          //java.util.Random...and create objet...
    int x = R.nextInt(100);  //range from 0 to 100...
    int x = R.nextInt((max-min)+1)+min;      //in range min to max...
    char c = S.charAt(R.nextInt(S.length()));  //To generate character...here S is a String with all alphabets...

int x = Arrays.asList(S).indexOf(c);        //To find index of String 'c' in String_array 'S'...

System.exit(0);          //java.lang.System; ...

public static int flag=0;    //static global variable declaration...

int[] M = new int[n];
    Arrays.fill(M,0);     //Initialize array with 0...

public static void arraycopy(Object src, int srcPos, Object dest, int destPos, int length)   // import java.lang.System...to shift arraycopy...System.arraycopy()

import java.math.BigInteger;
    BigInteger res = new BigInteger("1");   //initialized to 1...
    res = res.multiply(BigInteger.valueOf(i));     //here i is integer...